# Referência — não é o produto

Esta pasta veio do zip `rede-social` só como **modelo visual/funcional**.

O produto real é o **App Treino** em:

- `apps/web` (StudentPanel / Feed)
- `apps/mobile`
- `apps/api`

Não rode isto como app principal. Use `npm run dev:web` + `npm run dev:api`.
