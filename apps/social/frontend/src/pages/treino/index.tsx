import { AppTreinoBridge } from "@/components/treino/AppTreinoBridge";

export default function TreinoCatalogPage() {
  return (
    <AppTreinoBridge
      title="Treino"
      section="training"
      subtitle="Catálogo, programas, player e histórico do App Treino — preservados."
    />
  );
}
